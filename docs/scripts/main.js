(function ($,TweenMax) {
    'use strict';

    $ = $ && $.hasOwnProperty('default') ? $['default'] : $;

    $(function() {
        $('body').append('Hello again from JS!');
    });

}($,TweenMax));
